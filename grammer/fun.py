#!/usr/bin/python3

#two numbers as input
#output their average

def avg(x, y):
	print("first input is", x)
	print("second input is", y)
	a = (x + y) / 2.0
	print("average is ", a)
	return a
avg(45,56)
